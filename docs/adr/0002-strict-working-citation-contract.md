# Strict Working Citation Contract

The MVP treats citations as a product contract, not metadata decoration: a claim citation must jump to immutable artifact line ranges, the snippet must match the stored artifact text, and the verifier must classify support as supported, partial, or unsupported. This adds some validation and UI work, but it protects the core differentiator from becoming a superficial source label that technical users cannot trust.
